unique_companies_banks = {
	default = banks_enabled

	banks_enabled = {
	}

	banks_disabled = {
		flag = banks_disabled
	}
}
unique_companies_newspapers = {
	default = newspapers_enabled

	newspapers_enabled = {
	}

	newspapers_disabled = {
		flag = newspapers_disabled
	}
}